/* Add two numbers */
let y = 1
let x = 2
let add = x + y

/* Multiply two numbers */
let mul = x * y

/* Subtract one from a number */
2 - 1

/* Concatenate two strings */
let str1 = "Hello "
let str2 = "World"
let str3 = str1 + str2

/* Assign a value to a variable */
let c = 2

/* Increment the value of a variable by 3*/
c * 3

// Compare two values
c == y

// Check to see if a number is less than another number
c < y

// Check to see if two values are NOT equal
str1 != str2

// Check to see if a value is less than 10 and greater than 0
(x < 10 && x > 0)